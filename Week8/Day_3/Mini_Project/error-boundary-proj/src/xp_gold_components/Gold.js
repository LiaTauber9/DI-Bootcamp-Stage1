import { ErrorBoundary } from "./ErrorBoundary";
import { ErrorButton } from "./ErrorButton";

export const Gold = ()=>{
    return (
        // <ErrorBoundary>
        //     <ErrorButton />
        // </ErrorBoundary>
        <h1>hi</h1>
    )
}